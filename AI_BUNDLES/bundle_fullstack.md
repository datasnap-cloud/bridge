# Bundle: FULLSTACK

## System Architecture
- **datasnap-affiliates** (`affiliates`)
- **datasnap-backend** (`backend`)
- **datasnap-bridge** (`cli`)
- **datasnap-dataflow** (`dataflow`)
- **datasnap-frontend** (`frontend`)
- **datasnap-functions** (`functions`)
- **datasnap-infra** (`infra`)
- **datasnap-landing** (`landing`)
- **datasnap-docs** (`docs`)
